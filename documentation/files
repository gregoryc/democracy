files tool
"Files" is a simple command line computer program that lists all files in the current working directory. 

It takes exactly one argument, the directory from which to list files.

TODO

It only works on UNIX like system right now. (Linux + Mac + Windows with Windows Subsystem for Linux)



This will be fixed in the future.

"Files" is a simple command line computer program that lists all files in the current working directory. 

It takes exactly one argument, the directory from which to list files.

TODO

It only works on UNIX like system right now. (Linux + Mac + Windows with Windows Subsystem for Linux)



This will be fixed in the future.


