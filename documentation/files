files tool


"Files" is a simple command line computer program that lists all files in the current working directory.


It is kind of like a program called "find", but it is specifically geared towards files.


TODO 

It only works on Linux and UNIX (macOS) right now.

It works on Windows with Windows Subsystem for Linux as well.



"Files" is a simple command line computer program that lists all files in the current working directory.


It is kind of like a program called "find", but it is specifically geared towards files.


TODO

It only works on Linux and UNIX (macOS) right now.

It works on Windows with Windows Subsystem for Linux as well.


